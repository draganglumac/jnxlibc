IF (WIN32)
  set(PATH "win")
else()
  set(PATH "unix")
endif()

set(SOURCE ${SOURCE}
${CMAKE_CURRENT_SOURCE_DIR}/${PATH}/jnxstring.c
${CMAKE_CURRENT_SOURCE_DIR}/${PATH}/jnxterm.c
PARENT_SCOPE)

set(HEADERS
${HEADERS}
${CMAKE_CURRENT_SOURCE_DIR}/jnxstring.h
${CMAKE_CURRENT_SOURCE_DIR}/jnxterm.h
${CMAKE_CURRENT_SOURCE_DIR}/jnxtypes.h
PARENT_SCOPE)

set(LINKER_LIBRARIES
  ${LINKER_LIBRARIES}
  PARENT_SCOPE)
