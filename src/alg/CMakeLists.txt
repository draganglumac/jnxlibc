
IF (WIN32)
  set(PATH "win")
else()
  set(PATH "unix")
endif()

set(SOURCE ${SOURCE}
  ${CMAKE_CURRENT_SOURCE_DIR}/${PATH}/jnxencoder.c
  ${CMAKE_CURRENT_SOURCE_DIR}/${PATH}/jnxguid.c
  ${CMAKE_CURRENT_SOURCE_DIR}/${PATH}/jnxquicksort.c
PARENT_SCOPE)

set(HEADERS
${HEADERS}
${CMAKE_CURRENT_SOURCE_DIR}/jnxencoder.h
${CMAKE_CURRENT_SOURCE_DIR}/jnxguid.h
${CMAKE_CURRENT_SOURCE_DIR}/jnxquicksort.h
PARENT_SCOPE)

set(LINKER_LIBRARIES
  ${LINKER_LIBRARIES}
  PARENT_SCOPE)
