file(GLOB CURRENT_SRC "${CMAKE_CURRENT_SOURCE_DIR}/*.c")
file(GLOB CURRENT_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.h")

set(SOURCE ${CURRENT_SRC} PARENT_SCOPE)

set(HEADERS ${CURRENT_HEADERS} PARENT_SCOPE)
